name: Update poe.ninja data   # name of the robot task

on:
  schedule:
    - cron: "0 * * * *"   # run once every hour
  workflow_dispatch:      # also let me press "Run" button manually

jobs:
  update:
    runs-on: ubuntu-latest   # the robot will run on a Linux computer
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3   # pull your repo files so robot can change them

      - name: Fetch Fragments
        run: |
          curl -s "https://poe.ninja/api/data/currencyoverview?league=Mercenaries&type=Fragment" \
          -o docs/fragments.json   # save file into your repo/docs

      - name: Commit changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add docs/fragments.json
          git commit -m "Update fragments cache" || echo "No changes"
          git push
